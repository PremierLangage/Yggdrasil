** Pour pouvoir calculer l'intersection $!L(A1)\cap L(A2)!$ à partir du produit de deux automates $!A1!$ et $!A2!$ :
- $!A1!$ doit être déterministe;
- $!A1!$ doit être complet;
+ aucune contrainte sur $!A1!$;
- $!A2!$ doit être déterministe;
- $!A2!$ doit être complet;
+ aucune contrainte sur $!A2!$.
- Je ne sais pas répondre à cette question.

** Comment choisir les états finaux du produit de $!A1!$ et $!A2!$ afin qu'il reconnaisse le langage $!L(A1)\cap L(A2)!$?
- $! Q^1_{\scF}1\cup Qf2!$
+ $! Qf1xQf2 !$
- $! (Qf1,Qf2) !$
- $! Q1xQf2 U Qf1xQf2 !$
+ $! Q1xQf2 !$



** Pour pouvoir calculer la différence $!L(A1)\backslash L(A2)!$ à partir du produit de deux automates $!A1!$ et $!A2!$ :
- $!A1!$ doit être déterministe;
- $!A1!$ doit être complet;
+ aucune contrainte sur $!A1!$;
- $!A2!$ doit être déterministe;
- $!A2!$ doit être complet;
+ aucune contrainte sur $!A2!$.
- Je ne sais pas répondre à cette question.

** Pour pouvoir calculer l'union $!L(A1)\cup L(A2)!$ à partir du produit de deux automates $!A1!$ et $!A2!$ :
- $!A1!$ doit être déterministe;
- $!A1!$ doit être complet;
+ aucune contrainte sur $!A1!$;
- $!A2!$ doit être déterministe;
- $!A2!$ doit être complet;
+ aucune contrainte sur $!A2!$.
- Je ne sais pas répondre à cette question.


* Le produit de deux automates a toujours 1 seul état initial
-faux
+ vrai
- Je ne sais pas répondre à cette question.

* Le produit de deux automates déterministes a toujours 1 seul état initial
-faux
+ vrai
- Je ne sais pas répondre à cette question.

** Si A1 et A2 ont respectivement n1 et n2 état, l'automate obtenu par le produit de A1 et A2 possède:
- au moins n1 états accessibles
- au moins n1+n2 états accessibles
- au plus n1+n2 états accessibles
+ au plus n1*n2 états accessibles
- Je ne sais pas répondre à la question
