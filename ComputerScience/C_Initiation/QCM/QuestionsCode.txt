
Latex:1


**[group=separateur] 5 ) Quels sont les séparateurs dans un programme écrit en C ?

+ les espaces
+ les tabulations
+ les retours à la ligne
- les virgules
- les points virgules 

**[nb=2,group=separateur] 576567 ) Quels sont les séparateurs dans un programme écrit en C ?

+ les espaces
+ les tabulations
+ les retours à la ligne
- les virgules
- les points virgules 

**[nb=4,group=Resultat_code] 31 ) Qu'affiche la compilation **et/ou** l'exécution du programme suivant ?


     #include <stdio.h>

     int main(void){

        int j;

        int z;

        j = 5;

        z = j / 2;

        printf("%f \n", z);

        return 0;

     }

- 2.0
- 2
- 2.5
+ Une erreur ou un warning
- 75

**[nb=8,group=Resultat_code] 31 ) Qu'affiche la compilation **et/ou** l'exécution du programme suivant ?


     #include <stdio.h>

     int main(void){

        int j;

        int z;

        j = 5;

        z = j / 2;

        printf("%f \n", z);

        return 0;

     }

- 2.0
+ les espaces
- 2
+ les tabulations
- 2.5
+ les retours à la ligne
- les virgules
- les points virgules 

**[nb=8,group=Resultat_code] TEST) Qu'affiche la compilation **et/ou** l'exécution du programme suivant ?

```c
int ad_hash_init(int seed, int r, uint32_t *seeds, hash_t *hashes)
{
	int i, j, ssize = r%HIN128 ? r/HIN128+1 : r/HIN128;
	srand(seed);
    seeds = malloc(ssize * sizeof(uint32_t));
    for(i = 0; i < ssize; ++i) {/*seeds initialization*/
        j = rand();
        seeds[i] = *(uint32_t*)((void*)(&j));
    }
    hashes = malloc(ssize * HIN128 * sizeof(hash_t));
	return ssize;
}
```
- 2.0
+ les espaces
- 2
+ les tabulations
- 2.5

