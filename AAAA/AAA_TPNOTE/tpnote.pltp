




title = TP NOTE 

introduction ==




Dans certains exercice pour vous aider les arbres utilisés sont affiché (quand ils sont pas trop gros) avec la fonction suivante:

    void printTree(Arbre a){
        if (!a)
            printf("X ");
        else {
        printf("%d ", a->valeur);
        printTree(a->fg);
        printTree(a->fd);
        }
    }

==

@ alloc.pl
@ filsfrere.pl
@ filsfrere2.pl
@ pasfacile.pl
@ plusdur.pl
